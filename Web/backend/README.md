In the code, when you see the following path definition:

```python
BASE_DIR, "Models", "diffusion"
```

The diffusion model used here must be taken from the training results located in **Training/Diffusion**.